%% encrypt.m
% Implements a fake encryption scheme
% Author: Len Pieroni
% No assistance

%% Inputs
password = input('Enter password', 's');
phoneNumber = input('Enter phone number (just the digits)', 's');

%% Encrypt the thing


%% Output
out = strcat('The "encrypted" text is: \ ', encryption);
disp(out);